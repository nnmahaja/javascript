$('document').ready(function() {
    // not sure why this does not work immediately? boostrap?

    /*
    $('body').click(function() {
	// do something here like:
        alert('hey! The body click is working!!!')
    });
    */
    $('#divmain').click(function() {
	alert('divmain clicked')
    });
});
